# .env file for PythonAnywhere

# Security
SECRET_KEY='your-strong-secret-key-here'  # Generate new: openssl rand -base64 32
DEBUG='False'  # Always False in production

# Host configuration
PYTHONANYWHERE_USERNAME='yourusername'  # Replace with your PA username
ALLOWED_HOSTS='yourusername.pythonanywhere.com,localhost,127.0.0.1'

# Email configuration (if using)
EMAIL_HOST='smtp.gmail.com'
EMAIL_PORT='587'
EMAIL_USE_TLS='True'
EMAIL_HOST_USER='hakarsalih'
EMAIL_HOST_PASSWORD='123456789zz'
DEFAULT_FROM_EMAIL='noreply@yourdomain.com'

# Database (SQLite is used by default in free tier)
# No need for PostgreSQL config on free tier

# Optional: Add any other environment-specific variables here
